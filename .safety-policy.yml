# ARP Safety policy — always pass --policy-file .safety-policy.yml (or use make security).
#
# Streamlit CVEs below are accepted for the internal assessment dashboard; upgrades are
# tracked separately. Deployable image risk is gated by Trivy (HIGH/CRITICAL) in CI.
# See docs/DEPENDENCY_SCANNING.md.
security:
  ignore-vulnerabilities:
    88136:
      reason: Streamlit XSS — internal assessment dashboard only
    78762:
      reason: Streamlit CVE-2024-42474 — upgrade in roadmap
    90572:
      reason: Streamlit CVE-2026-33682 — upgrade in roadmap
